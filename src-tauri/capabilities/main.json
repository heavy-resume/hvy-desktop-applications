{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Permissions for the main HVY Galaxy desktop window.",
  "windows": ["main", "plugin-builder"],
  "permissions": [
    "core:default",
    "core:event:default",
    "core:event:allow-listen",
    "core:window:allow-set-theme",
    "core:window:allow-show"
  ]
}
